# Netlify configuration for the CRM frontend (Vite SPA)
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (deep links / refresh) resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
